#include <iostream>

using namespace std;

int main()
{
  double sum = 1;

  for (double A = 0;A < limit;A++)
  {
    for(double B = 1;B < A;B++)
    {
      if(A % B = 0)
      {
       sum = sum + B;
       if(sum = A)
       {
        cout << A;
       }
      }
    }
  }
}
